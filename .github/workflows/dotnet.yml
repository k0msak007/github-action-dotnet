# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Publish
      run: |
        dotnet publish --configuration Release --output ./publish

    - name: Archive published artifacts
      run: |
        ls
        cd publish
        tar -czvf published-artifacts.tar.gz *

        ls

  #   - name: Upload artifact
  #     uses: actions/upload-artifact@v2
  #     with:
  #       name: published-artifacts
  #       path: published-artifacts.tar.gz
  #       retention-days: 1

  #   - name: Path directory
  #     run: |
  #       ls
  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Checkout repository
  #     uses: actions/checkout@v2

  #   - name: Download published artifacts
  #     uses: actions/download-artifact@v2
  #     with:
  #       name: published-artifacts
  #       path: published-artifacts.tar.gz

  #   - name: Extract published artifacts
  #     run: |
  #       mkdir published-artifacts
  #       ls
  #       tar -xzvf published-artifacts.tar.gz -C published-artifacts